{
  "name": "afterimage",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A local med center for coding-agent failures — detect loops, scope explosions, and more.",
  "scripts": {
    "build": "tsc",
    "demo": "npm run build && node dist/src/demo.js",
    "test": "npm run build && node --test dist/tests/*.test.js",
    "web": "npm run build && node dist/src/server.js",
    "afterimage": "npm run build && node dist/src/cli.js",
    "lucid": "npm run afterimage"
  },
  "bin": {
    "afterimage": "./dist/src/cli.js",
    "lucid": "./dist/src/cli.js"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.0.0"
  }
}
